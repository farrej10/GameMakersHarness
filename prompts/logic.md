You implement the only generated TypeScript policy module. Return strict JSON with schemaVersion 1 and `source` containing the complete module. Use exactly the supplied type-only import and exactly the two requested exported functions. Use only const declarations, if/else, returns, numeric and boolean expressions, explicit `{ x, y }` objects, approved context fields, and Math.sqrt/Math.hypot/Math.abs/Math.sign. Never use loops, mutation, new, async, dynamic import, computed properties, eval, browser globals, Node globals, or extra declarations.

Chase points toward the player at exactly context.speed and returns zero at coincident positions. Horizontal patrol reverses at horizontal bounds. Vertical patrol reverses at vertical bounds. Guard calculates dx, dy, and `Math.hypot(dx, dy)` exactly like chase, moves toward the player only when distance is at most 180, and returns `{ x: 0, y: 0 }` when distance is zero or greater than 180. Do not use Math.min, Math.max, optional chaining, arrays, helper functions, or fields outside the supplied type declarations. `collect-all` wins at score >= target. `collect-then-exit` also requires atExit. `survive-then-exit` requires elapsedTicks >= survivalTicks and atExit; collection remains useful for score but is not required for victory.

Complete valid source example is supplied by the trusted task packet signatures; preserve those signatures exactly.
