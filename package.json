{
  "name": "agentic-game-maker-toolkit",
  "version": "1.0.0",
  "private": true,
  "description": "Generate, verify, and autonomously repair constrained browser games with OpenRouter workers.",
  "type": "module",
  "engines": {
    "node": ">=24.0.0 <25"
  },
  "scripts": {
    "dev": "vite",
    "dev:test": "vite --mode test --host 127.0.0.1",
    "dev:test:broken": "vite --mode test-broken-rules --host 127.0.0.1",
    "build": "npm run typecheck && vite build",
    "preview": "vite preview --host 127.0.0.1",
    "typecheck": "npm run typecheck:browser && npm run typecheck:node",
    "typecheck:browser": "tsc --project tsconfig.app.json --noEmit",
    "typecheck:node": "tsc --project tsconfig.node.json --noEmit",
    "test:unit": "vitest run --passWithNoTests",
    "test:browser": "playwright test",
    "game:serve": "node --import ./scripts/node-user-shim.mjs --import tsx src/toolkit/serve.ts",
    "verify": "node --import ./scripts/node-user-shim.mjs --import tsx scripts/verify.ts",
    "doctor": "node --import ./scripts/node-user-shim.mjs --import tsx scripts/doctor.ts",
    "check:public": "node scripts/check-public.mjs",
    "game:spec": "node --import ./scripts/node-user-shim.mjs --import tsx src/toolkit/cli.ts spec",
    "game:approve": "node --import ./scripts/node-user-shim.mjs --import tsx src/toolkit/cli.ts approve",
    "game:generate": "node --import ./scripts/node-user-shim.mjs --import tsx src/toolkit/cli.ts generate",
    "control": "node --import ./scripts/node-user-shim.mjs --import tsx src/toolkit/control.ts"
  },
  "license": "MIT",
  "dependencies": {
    "@sinclair/typebox": "0.34.52",
    "ajv": "8.20.0",
    "phaser": "3.90.0",
    "pngjs": "7.0.0"
  },
  "devDependencies": {
    "@types/node": "26.5.0",
    "@types/pngjs": "6.0.5",
    "playwright": "1.63.0",
    "tsx": "4.23.13",
    "typescript": "7.0.2",
    "vite": "8.2.2",
    "vitest": "5.0.0"
  }
}
